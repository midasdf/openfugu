# Decisions

## 2026-06-22: start with subscription-only core

The first implementation fixes the default execution policy to
subscription-only and rejects API-key, unauthenticated, and unknown auth.
